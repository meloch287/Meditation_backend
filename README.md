# Meditation_backend

Бэкенд система, разработанная на FastAPI, в целом **соответствует основным требованиям Технического Задания**.

## 1. 🛠 Технологии

* **Язык и фреймворк:** Python, FastAPI
* **База данных:** SQLite (для MVP, легко заменяется на PostgreSQL)
* **ORM:** SQLAlchemy
* **Управление зависимостями:** `requirements.txt` (включает `uvicorn`)
* **API ИИ:** OpenAI API (`gpt-4o-mini`)
* **Аутентификация:** Простая, основанная на `user_id`

## 2. 🏗 Архитектура бэкенд системы

```
meditation_backend/
├── requirements.txt
└── src/
    ├── database/
    │   └── app.db ( SQLite БД)
    ├── models/
    │   ├── models.py (определение моделей SQLAlchemy: User, Meditation, ActivationCode, ChatMessage)
    ├── routes/
    │   ├── chat_routes.py (эндпоинты для чата с ИИ-психологом)
    │   ├── meditation_routes.py (эндпоинты для управления медитациями)
    │   ├── subscription_routes.py (эндпоинты для управления подписками и кодами активации)
    │   └── user_routes.py (эндпоинты для управления пользователями)
    ├── static/
    │   ├── index.html (статическая страница)
    │   └── test_chat.html (страница для тест чата)
    └── main.py (FastAPI, регистрация роутов, статических файлов)
```

### 2.1. Взаимодействие компонентов

1.  **Клиентское приложение (Android)** отправляет HTTP-запросы к бэкенду FastAPI.
2.  **FastAPI** обрабатывает запросы, используя роутеры для различных функциональных областей.
3.  **Роутеры** взаимодействуют с базой данных через SQLAlchemy ORM для получения или сохранения данных.
4.  Для чата с психологом, `chat_routes.py` отправляет запросы к **OpenAI API** и сохраняет историю в БД.
5.  Статические файлы (`index.html`, `test_chat.html`) обслуживаются напрямую FastAPI.

## 3. 📌 Эндпоинты бэкенд системы

Все эндпоинты возвращают данные в формате JSON.

### 3.1. Общие эндпоинты

*   `GET /`
    *   Возвращает: `src/static/index.html`.
    *   Назначение: Главная статическая страница.
*   `GET /test-chat`
    *   Возвращает: `src/static/test_chat.html`.
    *   Назначение: Страница для тестирования функционала чата.
*   `GET /static/{filename}`
    *   Возвращает: Статические файлы из директории `src/static`.
    *   Назначение: Обслуживание статических ресурсов.

### 3.2. Эндпоинты для пользователей (`/api/users`)

| Метод | Путь | Описание | Входные данные (Body) | Выходные данные (JSON) |
|---|---|---|---|---|
| `GET` | `/` | Получить список всех пользователей. | Нет | `List[UserSchema]` |
| `GET` | `/{user_id}` | Получить информацию о конкретном пользователе. | Нет | `UserResponse` (включая `is_premium`, `premium_expires_at`, `last_played_meditation_id`) |
| `POST` | `/` | Создать нового пользователя. | `UserCreate` (`id: str`, `name: Optional[str]`) | `UserSchema` |
| `PUT` | `/{user_id}` | Обновить имя пользователя. | `UserUpdate` (`name: str`) | `UserSchema` |
| `DELETE` | `/{user_id}` | Удалить пользователя. | Нет | Статус 204 No Content |
| `POST` | `/{user_id}/last_played/{meditation_id}` | Обновить последнюю проигранную медитацию для пользователя. | Нет | `{"message": "Last played meditation updated", "last_played_meditation_id": int}` |
| `GET` | `/{user_id}/last_played` | Получить информацию о последней проигранной медитации пользователя. | Нет | `MeditationSchema` или `null` |
| `GET` | `/{user_id}/subscriptions` | Получить историю активаций кодов для пользователя. | Нет | `List[ActivationInfo]` |

### 3.3. Эндпоинты для медитаций (`/api/meditations`)

| Метод | Путь | Описание | Входные данные (Query/Body) | Выходные данные (JSON) |
|---|---|---|---|---|
| `POST` | `/seed` | Заполнить базу данных тестовыми медитациями. | Нет | `{"message": "Meditation data seeded successfully"}` |
| `GET` | `/` | Получить список медитаций. Может быть отфильтрован по категории. Учитывает Premium-статус пользователя. | `user_id: Optional[str]`, `category: Optional[str]` | `List[MeditationSchema]` |
| `GET` | `/{meditation_id}` | Получить информацию о конкретной медитации. Учитывает Premium-статус пользователя. | `user_id: Optional[str]` | `MeditationSchema` |

### 3.4. Эндпоинты для подписок (`/api/subscription`)

| Метод | Путь | Описание | Входные данные (Query/Body) | Выходные данные (JSON) |
|---|---|---|---|---|
| `GET` | `/check` | Проверить статус кода активации. | `code: str` | `ActivationCodeCheckResponse` (`status: "valid"/"used"/"not found"`, `expires_in: str`) |
| `POST` | `/activate` | Активировать код подписки для пользователя. | `ActivationCodeActivateRequest` (`code: str`, `user_id: str`) | `ActivationCodeActivateResponse` (`status: "activated"`, `until: datetime`) |
| `POST` | `/generate_code` | Сгенерировать новый код активации (для административных целей или сайта-оплаты). | `duration_days: int` (по умолчанию 30) | `{"raw_code": str, "hashed_code": str, "duration_days": int}` |
| `GET` | `/history` | Получить историю активаций кодов для пользователя. | `user_id: str` | `List[ActivationCodeHistoryResponse]` |

### 3.5. Эндпоинты для чата (`/api/chat`)

| Метод | Путь | Описание | Входные данные (Body) | Выходные данные (JSON) |
|---|---|---|---|---|
| `POST` | `/` | Отправить сообщение ИИ-психологу и получить ответ. Сохраняет историю чата. | `ChatRequest` (`user_id: str`, `message: str`) | `ChatResponse` (`response: str`) |
| `GET` | `/history` | Получить историю чата для пользователя. | `user_id: str` | `List[ChatResponse]` |

## 4. ⚙ Функционал

Данный бэкенд предоставляет следующие ключевые возможности:

*   **Управление пользователями:** Создание, получение, обновление и удаление профилей пользователей. Отслеживание имени и Premium-статуса.
*   **Каталог медитаций:** Предоставление списка медитаций с подробной информацией (название, описание, длительность, аудио-ссылка, категория). Возможность фильтрации по категориям.
*   **Premium-контент:** Разграничение доступа к медитациям на основе Premium-статуса пользователя.
*   **Система подписки на основе кодов:** Гибкая система активации Premium-доступа с использованием уникальных кодов. Включает проверку, активацию и генерацию кодов.
*   **История активаций:** Хранение и предоставление истории использования кодов активации для каждого пользователя.
*   **Чат с ИИ-психологом:** Интеграция с OpenAI API для создания интерактивного чата с эмпатичным ИИ-психологом, способным давать поддерживающие ответы. Сохранение полной истории диалога.
*   **Отслеживание прогресса:** Возможность сохранять и получать информацию о последней проигранной медитации для каждого пользователя.
*   **Статические файлы:** Обслуживание статических HTML-страниц, что может быть использовано для страниц оплаты или тестирования.
*   **Расширяемость:** Модульная структура на FastAPI облегчает добавление нового функционала и эндпоинтов.
*   **База данных:** Использование SQLAlchemy позволяет легко переключаться между различными СУБД (например, с SQLite на PostgreSQL) без значительных изменений в коде.

## 5. 📝 Установка 


### 1. Создание виртуального окружения

```bash
python -m venv venv
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```


### 3. Запуск бэка

```bash
uvicorn src.main:app --host 0.0.0.0 --port 8000
```

### 4. Запуск тестов с покрытием

```bash
pytest --cov=src tests/unit tests/integration
```

### 5. Тест Аи-чата (GPT)

1. Включаешь VPN ( в рф недоступен gpt + обязательно добавить апи ключ в env)
2. Запускаешь бэкенд (пункт 4).
3. Переходишь по адресу:

```
http://localhost:8000/test-chat
```

4. Если OpenAI client инициализирован и работает, он делает реальный вызов API. Если что-то пошло не так (например, нет API ключа или нет подключения), выбрасывается исключение, и блок except возвращает заранее заготовленный ответ

```
response_text = f"Это пример ответа ИИ на сообщение: '{request.message}'."
```

### 6. Тест создания, удаления и изменения пользователей через минимальный UI

Переходишь по адресу и дальше все интуитивно понятно:

```
http://localhost:8000
```




